# Factory Tools

This directory contains helper scripts used to enforce factory discipline.

These tools are used by CI and occasionally by operators.
They must never modify planning or execution state automatically.